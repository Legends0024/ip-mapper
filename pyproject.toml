[project]
name = "ip-geolocation-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.1.3",
    "requests>=2.33.1",
    "flask-cors>=4.0.0",
    "gunicorn>=25.3.0",
]
